name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version'
        required: true

concurrency:
  # On master, we don't want any jobs cancelled so the sha is used to name the group
  # On PR branches, we cancel the job if new commits are pushed
  # More info: https://stackoverflow.com/a/68422069/253468
  group: ${{ (github.ref == 'refs/heads/master') && format('{0}-{1}', github.workflow_ref, github.sha) || format('{0}-{1}', github.workflow_ref, github.head_ref) }}
  cancel-in-progress: true

env:
  JAVA_VERSION: '21'
  JAVA_DISTRO: 'adopt'

jobs:
  current-version:
    uses: ./.github/workflows/callable.current-version.yml
    if: |
      github.repository == 'tkaczenko/auditor' 
      && startsWith(github.event.head_commit.message, 'Releasing version') != true

  new-version:
    uses: ./.github/workflows/callable.new-version.yml
    if: |
      github.event_name == 'workflow_dispatch'
      && inputs.version != ''

  build:
    uses: ./.github/workflows/callable.build.yml

  publish:
    needs:
      - build
    uses: ./.github/workflows/callable.publish.yml